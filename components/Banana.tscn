[gd_scene load_steps=5 format=3]

[ext_resource type="Script" path="res://components/banana.gd" id="1"]

; Low-poly banana body: tapered cylinder (6-sided) pointing up.
[sub_resource type="CylinderMesh" id="CylinderMesh_1"]
top_radius = 0.04
bottom_radius = 0.14
height = 0.35
radial_segments = 6
rings = 1

; Bright yellow low-poly material – no textures needed.
[sub_resource type="StandardMaterial3D" id="Material_Banana"]
albedo_color = Color(1.0, 0.85, 0.05, 1)

; Generous pickup sphere – easier to grab mid-swing later.
[sub_resource type="SphereShape3D" id="SphereShape_1"]
radius = 0.9

; ─────────────────────────────────────────────────────────────────────────────
; ROOT  –  Node3D
;   The root moves (bob/spin) so every child moves with it automatically.
; ─────────────────────────────────────────────────────────────────────────────
[node name="Banana" type="Node3D"]
script = ExtResource("1")

; Visual representation.
[node name="MeshInstance3D" type="MeshInstance3D" parent="."]
mesh = SubResource("CylinderMesh_1")
surface_material_override/0 = SubResource("Material_Banana")

; ── Pickup trigger ────────────────────────────────────────────────────────────
; Area3D detects CharacterBody3D via body_entered.
; Monitoring must be ON (default). CollisionLayer/Mask are left at defaults;
; tweak them in the editor if you add physics layers later.
[node name="Area3D" type="Area3D" parent="."]

[node name="CollisionShape3D" type="CollisionShape3D" parent="Area3D"]
shape = SubResource("SphereShape_1")
